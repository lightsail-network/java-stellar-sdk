// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

package org.stellar.sdk.xdr;

import java.io.ByteArrayInputStream;
import java.io.IOException;
import java.util.LinkedHashMap;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;
import org.stellar.sdk.Base64Factory;

/**
 * DiagnosticEvent's original definition in the XDR file is:
 *
 * <pre>
 * struct DiagnosticEvent
 * {
 *     bool inSuccessfulContractCall;
 *     ContractEvent event;
 * };
 * </pre>
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
@Builder(toBuilder = true)
public class DiagnosticEvent implements XdrElement {
  private Boolean inSuccessfulContractCall;
  private ContractEvent event;

  public void encode(XdrDataOutputStream stream) throws IOException {
    stream.writeInt(inSuccessfulContractCall ? 1 : 0);
    event.encode(stream);
  }

  public static DiagnosticEvent decode(XdrDataInputStream stream, int maxDepth) throws IOException {
    if (maxDepth <= 0) {
      throw new IOException("Maximum decoding depth reached");
    }
    maxDepth -= 1;
    DiagnosticEvent decodedDiagnosticEvent = new DiagnosticEvent();
    decodedDiagnosticEvent.inSuccessfulContractCall = stream.readXdrBoolean();
    decodedDiagnosticEvent.event = ContractEvent.decode(stream, maxDepth);
    return decodedDiagnosticEvent;
  }

  public static DiagnosticEvent decode(XdrDataInputStream stream) throws IOException {
    return decode(stream, XdrDataInputStream.DEFAULT_MAX_DEPTH);
  }

  public static DiagnosticEvent fromXdrBase64(String xdr) throws IOException {
    byte[] bytes = Base64Factory.getInstance().decode(xdr);
    return fromXdrByteArray(bytes);
  }

  public static DiagnosticEvent fromXdrByteArray(byte[] xdr) throws IOException {
    ByteArrayInputStream byteArrayInputStream = new ByteArrayInputStream(xdr);
    XdrDataInputStream xdrDataInputStream = new XdrDataInputStream(byteArrayInputStream);
    xdrDataInputStream.setMaxInputLen(xdr.length);
    return decode(xdrDataInputStream);
  }

  @Override
  public String toJson() {
    return XdrElement.gson.toJson(toJsonObject());
  }

  public static DiagnosticEvent fromJson(String json) {
    return fromJsonObject(XdrElement.gson.fromJson(json, Object.class));
  }

  Object toJsonObject() {
    LinkedHashMap<String, Object> jsonMap = new LinkedHashMap<>();
    jsonMap.put("in_successful_contract_call", (Boolean) inSuccessfulContractCall);
    jsonMap.put("event", event.toJsonObject());
    return jsonMap;
  }

  @SuppressWarnings("unchecked")
  static DiagnosticEvent fromJsonObject(Object json) {
    java.util.Map<String, Object> jsonMap = (java.util.Map<String, Object>) json;
    DiagnosticEvent instance = new DiagnosticEvent();
    instance.inSuccessfulContractCall = (Boolean) jsonMap.get("in_successful_contract_call");
    instance.event = ContractEvent.fromJsonObject(jsonMap.get("event"));
    return instance;
  }
}
