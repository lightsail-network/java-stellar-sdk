// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

package org.stellar.sdk.xdr;

import java.io.ByteArrayInputStream;
import java.io.IOException;
import lombok.AllArgsConstructor;
import lombok.Data;
import lombok.NoArgsConstructor;
import org.stellar.sdk.Base64Factory;

/**
 * EncodedLedgerKey's original definition in the XDR file is:
 *
 * <pre>
 * typedef opaque EncodedLedgerKey&lt;&gt;;
 * </pre>
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
public class EncodedLedgerKey implements XdrElement {
  /**
   * Value of the {@code EncodedLedgerKey} field.
   *
   * @param EncodedLedgerKey the {@code EncodedLedgerKey} field value
   * @return the {@code EncodedLedgerKey} field value
   */
  private byte[] EncodedLedgerKey;

  public void encode(XdrDataOutputStream stream) throws IOException {
    int EncodedLedgerKeySize = EncodedLedgerKey.length;
    stream.writeInt(EncodedLedgerKeySize);
    stream.write(getEncodedLedgerKey(), 0, EncodedLedgerKeySize);
  }

  public static EncodedLedgerKey decode(XdrDataInputStream stream, int maxDepth)
      throws IOException {
    if (maxDepth <= 0) {
      throw new IOException("Maximum decoding depth reached");
    }
    maxDepth -= 1;
    EncodedLedgerKey decodedEncodedLedgerKey = new EncodedLedgerKey();
    int EncodedLedgerKeySize = stream.readInt();
    if (EncodedLedgerKeySize < 0) {
      throw new IOException("EncodedLedgerKey size " + EncodedLedgerKeySize + " is negative");
    }
    int EncodedLedgerKeyRemainingInputLen = stream.getRemainingInputLen();
    if (EncodedLedgerKeyRemainingInputLen >= 0
        && EncodedLedgerKeyRemainingInputLen < EncodedLedgerKeySize) {
      throw new IOException(
          "EncodedLedgerKey size "
              + EncodedLedgerKeySize
              + " exceeds remaining input length "
              + EncodedLedgerKeyRemainingInputLen);
    }
    decodedEncodedLedgerKey.EncodedLedgerKey = new byte[EncodedLedgerKeySize];
    stream.readPaddedData(decodedEncodedLedgerKey.EncodedLedgerKey, 0, EncodedLedgerKeySize);
    return decodedEncodedLedgerKey;
  }

  public static EncodedLedgerKey decode(XdrDataInputStream stream) throws IOException {
    return decode(stream, XdrDataInputStream.DEFAULT_MAX_DEPTH);
  }

  public static EncodedLedgerKey fromXdrBase64(String xdr) throws IOException {
    byte[] bytes = Base64Factory.getInstance().decode(xdr);
    return fromXdrByteArray(bytes);
  }

  public static EncodedLedgerKey fromXdrByteArray(byte[] xdr) throws IOException {
    ByteArrayInputStream byteArrayInputStream = new ByteArrayInputStream(xdr);
    XdrDataInputStream xdrDataInputStream = new XdrDataInputStream(byteArrayInputStream);
    xdrDataInputStream.setMaxInputLen(xdr.length);
    return decode(xdrDataInputStream);
  }

  @Override
  public String toJson() {
    return XdrElement.gson.toJson(toJsonObject());
  }

  public static EncodedLedgerKey fromJson(String json) {
    return fromJsonObject(XdrElement.gson.fromJson(json, Object.class));
  }

  Object toJsonObject() {
    return XdrElement.bytesToHex(EncodedLedgerKey);
  }

  static EncodedLedgerKey fromJsonObject(Object json) {
    EncodedLedgerKey instance = new EncodedLedgerKey();
    instance.EncodedLedgerKey = XdrElement.hexToBytes((String) json);
    return instance;
  }
}
