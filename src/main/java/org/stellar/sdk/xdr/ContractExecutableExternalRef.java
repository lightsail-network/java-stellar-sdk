// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

package org.stellar.sdk.xdr;

import java.io.ByteArrayInputStream;
import java.io.IOException;
import java.util.LinkedHashMap;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;
import org.stellar.sdk.Base64Factory;

/**
 * ContractExecutableExternalRef's original definition in the XDR file is:
 *
 * <pre>
 * struct ContractExecutableExternalRef {
 *     SCAddress executable_owner;
 *     SCString tag;
 * };
 * </pre>
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
@Builder(toBuilder = true)
public class ContractExecutableExternalRef implements XdrElement {
  /**
   * Value of the {@code executable_owner} field.
   *
   * @param executable_owner the {@code executable_owner} field value
   * @return the {@code executable_owner} field value
   */
  private SCAddress executable_owner;

  /**
   * Value of the {@code tag} field.
   *
   * @param tag the {@code tag} field value
   * @return the {@code tag} field value
   */
  private SCString tag;

  public void encode(XdrDataOutputStream stream) throws IOException {
    executable_owner.encode(stream);
    tag.encode(stream);
  }

  public static ContractExecutableExternalRef decode(XdrDataInputStream stream, int maxDepth)
      throws IOException {
    if (maxDepth <= 0) {
      throw new IOException("Maximum decoding depth reached");
    }
    maxDepth -= 1;
    ContractExecutableExternalRef decodedContractExecutableExternalRef =
        new ContractExecutableExternalRef();
    decodedContractExecutableExternalRef.executable_owner = SCAddress.decode(stream, maxDepth);
    decodedContractExecutableExternalRef.tag = SCString.decode(stream, maxDepth);
    return decodedContractExecutableExternalRef;
  }

  public static ContractExecutableExternalRef decode(XdrDataInputStream stream) throws IOException {
    return decode(stream, XdrDataInputStream.DEFAULT_MAX_DEPTH);
  }

  public static ContractExecutableExternalRef fromXdrBase64(String xdr) throws IOException {
    byte[] bytes = Base64Factory.getInstance().decode(xdr);
    return fromXdrByteArray(bytes);
  }

  public static ContractExecutableExternalRef fromXdrByteArray(byte[] xdr) throws IOException {
    ByteArrayInputStream byteArrayInputStream = new ByteArrayInputStream(xdr);
    XdrDataInputStream xdrDataInputStream = new XdrDataInputStream(byteArrayInputStream);
    xdrDataInputStream.setMaxInputLen(xdr.length);
    return decode(xdrDataInputStream);
  }

  @Override
  public String toJson() {
    return XdrElement.gson.toJson(toJsonObject());
  }

  public static ContractExecutableExternalRef fromJson(String json) {
    return fromJsonObject(XdrElement.gson.fromJson(json, Object.class));
  }

  Object toJsonObject() {
    LinkedHashMap<String, Object> jsonMap = new LinkedHashMap<>();
    jsonMap.put("executable_owner", executable_owner.toJsonObject());
    jsonMap.put("tag", tag.toJsonObject());
    return jsonMap;
  }

  @SuppressWarnings("unchecked")
  static ContractExecutableExternalRef fromJsonObject(Object json) {
    java.util.Map<String, Object> jsonMap = (java.util.Map<String, Object>) json;
    ContractExecutableExternalRef instance = new ContractExecutableExternalRef();
    instance.executable_owner = SCAddress.fromJsonObject(jsonMap.get("executable_owner"));
    instance.tag = SCString.fromJsonObject(jsonMap.get("tag"));
    return instance;
  }
}
