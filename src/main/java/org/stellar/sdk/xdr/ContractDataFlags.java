// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

package org.stellar.sdk.xdr;

import static org.stellar.sdk.xdr.Constants.*;

import java.io.IOException;

// === xdr source ============================================================

//  enum ContractDataFlags {
//      // When set, the given entry does not recieve automatic expiration bumps
//      // on access. Note that entries can still be bumped manually via the footprint.
//      NO_AUTOBUMP = 0x1
//  };

//  ===========================================================================
public enum ContractDataFlags implements XdrElement {
  NO_AUTOBUMP(1),
  ;
  private int mValue;

  ContractDataFlags(int value) {
    mValue = value;
  }

  public int getValue() {
    return mValue;
  }

  public static ContractDataFlags decode(XdrDataInputStream stream) throws IOException {
    int value = stream.readInt();
    switch (value) {
      case 1:
        return NO_AUTOBUMP;
      default:
        throw new RuntimeException("Unknown enum value: " + value);
    }
  }

  public static void encode(XdrDataOutputStream stream, ContractDataFlags value)
      throws IOException {
    stream.writeInt(value.getValue());
  }

  public void encode(XdrDataOutputStream stream) throws IOException {
    encode(stream, this);
  }
}
