// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

package org.stellar.sdk.xdr;

import java.io.ByteArrayInputStream;
import java.io.IOException;
import java.util.LinkedHashMap;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;
import org.stellar.sdk.Base64Factory;

/**
 * Price's original definition in the XDR file is:
 *
 * <pre>
 * struct Price
 * {
 *     int32 n; // numerator
 *     int32 d; // denominator
 * };
 * </pre>
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
@Builder(toBuilder = true)
public class Price implements XdrElement {
  private Int32 n;
  private Int32 d;

  public void encode(XdrDataOutputStream stream) throws IOException {
    n.encode(stream);
    d.encode(stream);
  }

  public static Price decode(XdrDataInputStream stream, int maxDepth) throws IOException {
    if (maxDepth <= 0) {
      throw new IOException("Maximum decoding depth reached");
    }
    maxDepth -= 1;
    Price decodedPrice = new Price();
    decodedPrice.n = Int32.decode(stream, maxDepth);
    decodedPrice.d = Int32.decode(stream, maxDepth);
    return decodedPrice;
  }

  public static Price decode(XdrDataInputStream stream) throws IOException {
    return decode(stream, XdrDataInputStream.DEFAULT_MAX_DEPTH);
  }

  public static Price fromXdrBase64(String xdr) throws IOException {
    byte[] bytes = Base64Factory.getInstance().decode(xdr);
    return fromXdrByteArray(bytes);
  }

  public static Price fromXdrByteArray(byte[] xdr) throws IOException {
    ByteArrayInputStream byteArrayInputStream = new ByteArrayInputStream(xdr);
    XdrDataInputStream xdrDataInputStream = new XdrDataInputStream(byteArrayInputStream);
    xdrDataInputStream.setMaxInputLen(xdr.length);
    return decode(xdrDataInputStream);
  }

  @Override
  public String toJson() {
    return XdrElement.gson.toJson(toJsonObject());
  }

  public static Price fromJson(String json) {
    return fromJsonObject(XdrElement.gson.fromJson(json, Object.class));
  }

  Object toJsonObject() {
    LinkedHashMap<String, Object> jsonMap = new LinkedHashMap<>();
    jsonMap.put("n", n.toJsonObject());
    jsonMap.put("d", d.toJsonObject());
    return jsonMap;
  }

  @SuppressWarnings("unchecked")
  static Price fromJsonObject(Object json) {
    java.util.Map<String, Object> jsonMap = (java.util.Map<String, Object>) json;
    Price instance = new Price();
    instance.n = Int32.fromJsonObject(jsonMap.get("n"));
    instance.d = Int32.fromJsonObject(jsonMap.get("d"));
    return instance;
  }
}
