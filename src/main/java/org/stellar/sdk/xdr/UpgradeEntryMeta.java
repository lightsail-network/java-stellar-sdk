// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

package org.stellar.sdk.xdr;

import java.io.ByteArrayInputStream;
import java.io.IOException;
import java.util.LinkedHashMap;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;
import org.stellar.sdk.Base64Factory;

/**
 * UpgradeEntryMeta's original definition in the XDR file is:
 *
 * <pre>
 * struct UpgradeEntryMeta
 * {
 *     LedgerUpgrade upgrade;
 *     LedgerEntryChanges changes;
 * };
 * </pre>
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
@Builder(toBuilder = true)
public class UpgradeEntryMeta implements XdrElement {
  private LedgerUpgrade upgrade;
  private LedgerEntryChanges changes;

  public void encode(XdrDataOutputStream stream) throws IOException {
    upgrade.encode(stream);
    changes.encode(stream);
  }

  public static UpgradeEntryMeta decode(XdrDataInputStream stream, int maxDepth)
      throws IOException {
    if (maxDepth <= 0) {
      throw new IOException("Maximum decoding depth reached");
    }
    maxDepth -= 1;
    UpgradeEntryMeta decodedUpgradeEntryMeta = new UpgradeEntryMeta();
    decodedUpgradeEntryMeta.upgrade = LedgerUpgrade.decode(stream, maxDepth);
    decodedUpgradeEntryMeta.changes = LedgerEntryChanges.decode(stream, maxDepth);
    return decodedUpgradeEntryMeta;
  }

  public static UpgradeEntryMeta decode(XdrDataInputStream stream) throws IOException {
    return decode(stream, XdrDataInputStream.DEFAULT_MAX_DEPTH);
  }

  public static UpgradeEntryMeta fromXdrBase64(String xdr) throws IOException {
    byte[] bytes = Base64Factory.getInstance().decode(xdr);
    return fromXdrByteArray(bytes);
  }

  public static UpgradeEntryMeta fromXdrByteArray(byte[] xdr) throws IOException {
    ByteArrayInputStream byteArrayInputStream = new ByteArrayInputStream(xdr);
    XdrDataInputStream xdrDataInputStream = new XdrDataInputStream(byteArrayInputStream);
    xdrDataInputStream.setMaxInputLen(xdr.length);
    return decode(xdrDataInputStream);
  }

  @Override
  public String toJson() {
    return XdrElement.gson.toJson(toJsonObject());
  }

  public static UpgradeEntryMeta fromJson(String json) {
    return fromJsonObject(XdrElement.gson.fromJson(json, Object.class));
  }

  Object toJsonObject() {
    LinkedHashMap<String, Object> jsonMap = new LinkedHashMap<>();
    jsonMap.put("upgrade", upgrade.toJsonObject());
    jsonMap.put("changes", changes.toJsonObject());
    return jsonMap;
  }

  @SuppressWarnings("unchecked")
  static UpgradeEntryMeta fromJsonObject(Object json) {
    java.util.Map<String, Object> jsonMap = (java.util.Map<String, Object>) json;
    UpgradeEntryMeta instance = new UpgradeEntryMeta();
    instance.upgrade = LedgerUpgrade.fromJsonObject(jsonMap.get("upgrade"));
    instance.changes = LedgerEntryChanges.fromJsonObject(jsonMap.get("changes"));
    return instance;
  }
}
