// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

package org.stellar.sdk.xdr;

import java.io.ByteArrayInputStream;
import java.io.IOException;
import java.util.LinkedHashMap;
import java.util.List;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;
import org.stellar.sdk.Base64Factory;

/**
 * FrozenLedgerKeys's original definition in the XDR file is:
 *
 * <pre>
 * struct FrozenLedgerKeys {
 *     EncodedLedgerKey keys&lt;&gt;;
 * };
 * </pre>
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
@Builder(toBuilder = true)
public class FrozenLedgerKeys implements XdrElement {
  /**
   * Value of the {@code keys} field.
   *
   * @param keys the {@code keys} field value
   * @return the {@code keys} field value
   */
  private EncodedLedgerKey[] keys;

  public void encode(XdrDataOutputStream stream) throws IOException {
    int keysSize = getKeys().length;
    stream.writeInt(keysSize);
    for (int i = 0; i < keysSize; i++) {
      keys[i].encode(stream);
    }
  }

  public static FrozenLedgerKeys decode(XdrDataInputStream stream, int maxDepth)
      throws IOException {
    if (maxDepth <= 0) {
      throw new IOException("Maximum decoding depth reached");
    }
    maxDepth -= 1;
    FrozenLedgerKeys decodedFrozenLedgerKeys = new FrozenLedgerKeys();
    int keysSize = stream.readInt();
    if (keysSize < 0) {
      throw new IOException("keys size " + keysSize + " is negative");
    }
    int keysRemainingInputLen = stream.getRemainingInputLen();
    if (keysRemainingInputLen >= 0 && keysRemainingInputLen < keysSize) {
      throw new IOException(
          "keys size " + keysSize + " exceeds remaining input length " + keysRemainingInputLen);
    }
    decodedFrozenLedgerKeys.keys = new EncodedLedgerKey[keysSize];
    for (int i = 0; i < keysSize; i++) {
      decodedFrozenLedgerKeys.keys[i] = EncodedLedgerKey.decode(stream, maxDepth);
    }
    return decodedFrozenLedgerKeys;
  }

  public static FrozenLedgerKeys decode(XdrDataInputStream stream) throws IOException {
    return decode(stream, XdrDataInputStream.DEFAULT_MAX_DEPTH);
  }

  public static FrozenLedgerKeys fromXdrBase64(String xdr) throws IOException {
    byte[] bytes = Base64Factory.getInstance().decode(xdr);
    return fromXdrByteArray(bytes);
  }

  public static FrozenLedgerKeys fromXdrByteArray(byte[] xdr) throws IOException {
    ByteArrayInputStream byteArrayInputStream = new ByteArrayInputStream(xdr);
    XdrDataInputStream xdrDataInputStream = new XdrDataInputStream(byteArrayInputStream);
    xdrDataInputStream.setMaxInputLen(xdr.length);
    return decode(xdrDataInputStream);
  }

  @Override
  public String toJson() {
    return XdrElement.gson.toJson(toJsonObject());
  }

  public static FrozenLedgerKeys fromJson(String json) {
    return fromJsonObject(XdrElement.gson.fromJson(json, Object.class));
  }

  Object toJsonObject() {
    LinkedHashMap<String, Object> jsonMap = new LinkedHashMap<>();
    jsonMap.put("keys", XdrElement.arrayToJsonArray(keys, i -> keys[i].toJsonObject()));
    return jsonMap;
  }

  @SuppressWarnings("unchecked")
  static FrozenLedgerKeys fromJsonObject(Object json) {
    java.util.Map<String, Object> jsonMap = (java.util.Map<String, Object>) json;
    FrozenLedgerKeys instance = new FrozenLedgerKeys();
    instance.keys =
        XdrElement.jsonArrayToArray(
            (List<Object>) jsonMap.get("keys"),
            EncodedLedgerKey.class,
            item -> EncodedLedgerKey.fromJsonObject(item));
    return instance;
  }
}
