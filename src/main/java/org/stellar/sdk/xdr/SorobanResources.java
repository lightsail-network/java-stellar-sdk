// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

package org.stellar.sdk.xdr;

import java.io.ByteArrayInputStream;
import java.io.IOException;
import java.util.LinkedHashMap;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;
import org.stellar.sdk.Base64Factory;

/**
 * SorobanResources's original definition in the XDR file is:
 *
 * <pre>
 * struct SorobanResources
 * {
 *     // The ledger footprint of the transaction.
 *     LedgerFootprint footprint;
 *     // The maximum number of instructions this transaction can use
 *     uint32 instructions;
 *
 *     // The maximum number of bytes this transaction can read from disk backed entries
 *     uint32 diskReadBytes;
 *     // The maximum number of bytes this transaction can write to ledger
 *     uint32 writeBytes;
 * };
 * </pre>
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
@Builder(toBuilder = true)
public class SorobanResources implements XdrElement {
  private LedgerFootprint footprint;
  private Uint32 instructions;
  private Uint32 diskReadBytes;
  private Uint32 writeBytes;

  public void encode(XdrDataOutputStream stream) throws IOException {
    footprint.encode(stream);
    instructions.encode(stream);
    diskReadBytes.encode(stream);
    writeBytes.encode(stream);
  }

  public static SorobanResources decode(XdrDataInputStream stream, int maxDepth)
      throws IOException {
    if (maxDepth <= 0) {
      throw new IOException("Maximum decoding depth reached");
    }
    maxDepth -= 1;
    SorobanResources decodedSorobanResources = new SorobanResources();
    decodedSorobanResources.footprint = LedgerFootprint.decode(stream, maxDepth);
    decodedSorobanResources.instructions = Uint32.decode(stream, maxDepth);
    decodedSorobanResources.diskReadBytes = Uint32.decode(stream, maxDepth);
    decodedSorobanResources.writeBytes = Uint32.decode(stream, maxDepth);
    return decodedSorobanResources;
  }

  public static SorobanResources decode(XdrDataInputStream stream) throws IOException {
    return decode(stream, XdrDataInputStream.DEFAULT_MAX_DEPTH);
  }

  public static SorobanResources fromXdrBase64(String xdr) throws IOException {
    byte[] bytes = Base64Factory.getInstance().decode(xdr);
    return fromXdrByteArray(bytes);
  }

  public static SorobanResources fromXdrByteArray(byte[] xdr) throws IOException {
    ByteArrayInputStream byteArrayInputStream = new ByteArrayInputStream(xdr);
    XdrDataInputStream xdrDataInputStream = new XdrDataInputStream(byteArrayInputStream);
    xdrDataInputStream.setMaxInputLen(xdr.length);
    return decode(xdrDataInputStream);
  }

  @Override
  public String toJson() {
    return XdrElement.gson.toJson(toJsonObject());
  }

  public static SorobanResources fromJson(String json) {
    return fromJsonObject(XdrElement.gson.fromJson(json, Object.class));
  }

  Object toJsonObject() {
    LinkedHashMap<String, Object> jsonMap = new LinkedHashMap<>();
    jsonMap.put("footprint", footprint.toJsonObject());
    jsonMap.put("instructions", instructions.toJsonObject());
    jsonMap.put("disk_read_bytes", diskReadBytes.toJsonObject());
    jsonMap.put("write_bytes", writeBytes.toJsonObject());
    return jsonMap;
  }

  @SuppressWarnings("unchecked")
  static SorobanResources fromJsonObject(Object json) {
    java.util.Map<String, Object> jsonMap = (java.util.Map<String, Object>) json;
    SorobanResources instance = new SorobanResources();
    instance.footprint = LedgerFootprint.fromJsonObject(jsonMap.get("footprint"));
    instance.instructions = Uint32.fromJsonObject(jsonMap.get("instructions"));
    instance.diskReadBytes = Uint32.fromJsonObject(jsonMap.get("disk_read_bytes"));
    instance.writeBytes = Uint32.fromJsonObject(jsonMap.get("write_bytes"));
    return instance;
  }
}
