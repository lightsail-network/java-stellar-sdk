// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

package org.stellar.sdk.xdr;

import java.io.ByteArrayInputStream;
import java.io.IOException;
import java.util.LinkedHashMap;
import java.util.List;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;
import org.stellar.sdk.Base64Factory;

/**
 * FrozenLedgerKeysDelta's original definition in the XDR file is:
 *
 * <pre>
 * struct FrozenLedgerKeysDelta {
 *     EncodedLedgerKey keysToFreeze&lt;&gt;;
 *     EncodedLedgerKey keysToUnfreeze&lt;&gt;;
 * };
 * </pre>
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
@Builder(toBuilder = true)
public class FrozenLedgerKeysDelta implements XdrElement {
  private EncodedLedgerKey[] keysToFreeze;
  private EncodedLedgerKey[] keysToUnfreeze;

  public void encode(XdrDataOutputStream stream) throws IOException {
    int keysToFreezeSize = getKeysToFreeze().length;
    stream.writeInt(keysToFreezeSize);
    for (int i = 0; i < keysToFreezeSize; i++) {
      keysToFreeze[i].encode(stream);
    }
    int keysToUnfreezeSize = getKeysToUnfreeze().length;
    stream.writeInt(keysToUnfreezeSize);
    for (int i = 0; i < keysToUnfreezeSize; i++) {
      keysToUnfreeze[i].encode(stream);
    }
  }

  public static FrozenLedgerKeysDelta decode(XdrDataInputStream stream, int maxDepth)
      throws IOException {
    if (maxDepth <= 0) {
      throw new IOException("Maximum decoding depth reached");
    }
    maxDepth -= 1;
    FrozenLedgerKeysDelta decodedFrozenLedgerKeysDelta = new FrozenLedgerKeysDelta();
    int keysToFreezeSize = stream.readInt();
    if (keysToFreezeSize < 0) {
      throw new IOException("keysToFreeze size " + keysToFreezeSize + " is negative");
    }
    int keysToFreezeRemainingInputLen = stream.getRemainingInputLen();
    if (keysToFreezeRemainingInputLen >= 0 && keysToFreezeRemainingInputLen < keysToFreezeSize) {
      throw new IOException(
          "keysToFreeze size "
              + keysToFreezeSize
              + " exceeds remaining input length "
              + keysToFreezeRemainingInputLen);
    }
    decodedFrozenLedgerKeysDelta.keysToFreeze = new EncodedLedgerKey[keysToFreezeSize];
    for (int i = 0; i < keysToFreezeSize; i++) {
      decodedFrozenLedgerKeysDelta.keysToFreeze[i] = EncodedLedgerKey.decode(stream, maxDepth);
    }
    int keysToUnfreezeSize = stream.readInt();
    if (keysToUnfreezeSize < 0) {
      throw new IOException("keysToUnfreeze size " + keysToUnfreezeSize + " is negative");
    }
    int keysToUnfreezeRemainingInputLen = stream.getRemainingInputLen();
    if (keysToUnfreezeRemainingInputLen >= 0
        && keysToUnfreezeRemainingInputLen < keysToUnfreezeSize) {
      throw new IOException(
          "keysToUnfreeze size "
              + keysToUnfreezeSize
              + " exceeds remaining input length "
              + keysToUnfreezeRemainingInputLen);
    }
    decodedFrozenLedgerKeysDelta.keysToUnfreeze = new EncodedLedgerKey[keysToUnfreezeSize];
    for (int i = 0; i < keysToUnfreezeSize; i++) {
      decodedFrozenLedgerKeysDelta.keysToUnfreeze[i] = EncodedLedgerKey.decode(stream, maxDepth);
    }
    return decodedFrozenLedgerKeysDelta;
  }

  public static FrozenLedgerKeysDelta decode(XdrDataInputStream stream) throws IOException {
    return decode(stream, XdrDataInputStream.DEFAULT_MAX_DEPTH);
  }

  public static FrozenLedgerKeysDelta fromXdrBase64(String xdr) throws IOException {
    byte[] bytes = Base64Factory.getInstance().decode(xdr);
    return fromXdrByteArray(bytes);
  }

  public static FrozenLedgerKeysDelta fromXdrByteArray(byte[] xdr) throws IOException {
    ByteArrayInputStream byteArrayInputStream = new ByteArrayInputStream(xdr);
    XdrDataInputStream xdrDataInputStream = new XdrDataInputStream(byteArrayInputStream);
    xdrDataInputStream.setMaxInputLen(xdr.length);
    return decode(xdrDataInputStream);
  }

  @Override
  public String toJson() {
    return XdrElement.gson.toJson(toJsonObject());
  }

  public static FrozenLedgerKeysDelta fromJson(String json) {
    return fromJsonObject(XdrElement.gson.fromJson(json, Object.class));
  }

  Object toJsonObject() {
    LinkedHashMap<String, Object> jsonMap = new LinkedHashMap<>();
    jsonMap.put(
        "keys_to_freeze",
        XdrElement.arrayToJsonArray(keysToFreeze, i -> keysToFreeze[i].toJsonObject()));
    jsonMap.put(
        "keys_to_unfreeze",
        XdrElement.arrayToJsonArray(keysToUnfreeze, i -> keysToUnfreeze[i].toJsonObject()));
    return jsonMap;
  }

  @SuppressWarnings("unchecked")
  static FrozenLedgerKeysDelta fromJsonObject(Object json) {
    java.util.Map<String, Object> jsonMap = (java.util.Map<String, Object>) json;
    FrozenLedgerKeysDelta instance = new FrozenLedgerKeysDelta();
    instance.keysToFreeze =
        XdrElement.jsonArrayToArray(
            (List<Object>) jsonMap.get("keys_to_freeze"),
            EncodedLedgerKey.class,
            item -> EncodedLedgerKey.fromJsonObject(item));
    instance.keysToUnfreeze =
        XdrElement.jsonArrayToArray(
            (List<Object>) jsonMap.get("keys_to_unfreeze"),
            EncodedLedgerKey.class,
            item -> EncodedLedgerKey.fromJsonObject(item));
    return instance;
  }
}
