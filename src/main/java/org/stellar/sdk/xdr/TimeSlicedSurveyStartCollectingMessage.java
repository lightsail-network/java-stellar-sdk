// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

package org.stellar.sdk.xdr;

import java.io.ByteArrayInputStream;
import java.io.IOException;
import java.util.LinkedHashMap;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;
import org.stellar.sdk.Base64Factory;

/**
 * TimeSlicedSurveyStartCollectingMessage's original definition in the XDR file is:
 *
 * <pre>
 * struct TimeSlicedSurveyStartCollectingMessage
 * {
 *     NodeID surveyorID;
 *     uint32 nonce;
 *     uint32 ledgerNum;
 * };
 * </pre>
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
@Builder(toBuilder = true)
public class TimeSlicedSurveyStartCollectingMessage implements XdrElement {
  private NodeID surveyorID;
  private Uint32 nonce;
  private Uint32 ledgerNum;

  public void encode(XdrDataOutputStream stream) throws IOException {
    surveyorID.encode(stream);
    nonce.encode(stream);
    ledgerNum.encode(stream);
  }

  public static TimeSlicedSurveyStartCollectingMessage decode(
      XdrDataInputStream stream, int maxDepth) throws IOException {
    if (maxDepth <= 0) {
      throw new IOException("Maximum decoding depth reached");
    }
    maxDepth -= 1;
    TimeSlicedSurveyStartCollectingMessage decodedTimeSlicedSurveyStartCollectingMessage =
        new TimeSlicedSurveyStartCollectingMessage();
    decodedTimeSlicedSurveyStartCollectingMessage.surveyorID = NodeID.decode(stream, maxDepth);
    decodedTimeSlicedSurveyStartCollectingMessage.nonce = Uint32.decode(stream, maxDepth);
    decodedTimeSlicedSurveyStartCollectingMessage.ledgerNum = Uint32.decode(stream, maxDepth);
    return decodedTimeSlicedSurveyStartCollectingMessage;
  }

  public static TimeSlicedSurveyStartCollectingMessage decode(XdrDataInputStream stream)
      throws IOException {
    return decode(stream, XdrDataInputStream.DEFAULT_MAX_DEPTH);
  }

  public static TimeSlicedSurveyStartCollectingMessage fromXdrBase64(String xdr)
      throws IOException {
    byte[] bytes = Base64Factory.getInstance().decode(xdr);
    return fromXdrByteArray(bytes);
  }

  public static TimeSlicedSurveyStartCollectingMessage fromXdrByteArray(byte[] xdr)
      throws IOException {
    ByteArrayInputStream byteArrayInputStream = new ByteArrayInputStream(xdr);
    XdrDataInputStream xdrDataInputStream = new XdrDataInputStream(byteArrayInputStream);
    xdrDataInputStream.setMaxInputLen(xdr.length);
    return decode(xdrDataInputStream);
  }

  @Override
  public String toJson() {
    return XdrElement.gson.toJson(toJsonObject());
  }

  public static TimeSlicedSurveyStartCollectingMessage fromJson(String json) {
    return fromJsonObject(XdrElement.gson.fromJson(json, Object.class));
  }

  Object toJsonObject() {
    LinkedHashMap<String, Object> jsonMap = new LinkedHashMap<>();
    jsonMap.put("surveyor_id", surveyorID.toJsonObject());
    jsonMap.put("nonce", nonce.toJsonObject());
    jsonMap.put("ledger_num", ledgerNum.toJsonObject());
    return jsonMap;
  }

  @SuppressWarnings("unchecked")
  static TimeSlicedSurveyStartCollectingMessage fromJsonObject(Object json) {
    java.util.Map<String, Object> jsonMap = (java.util.Map<String, Object>) json;
    TimeSlicedSurveyStartCollectingMessage instance = new TimeSlicedSurveyStartCollectingMessage();
    instance.surveyorID = NodeID.fromJsonObject(jsonMap.get("surveyor_id"));
    instance.nonce = Uint32.fromJsonObject(jsonMap.get("nonce"));
    instance.ledgerNum = Uint32.fromJsonObject(jsonMap.get("ledger_num"));
    return instance;
  }
}
