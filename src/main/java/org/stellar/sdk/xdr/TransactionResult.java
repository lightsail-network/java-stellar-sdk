// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

package org.stellar.sdk.xdr;

import java.io.ByteArrayInputStream;
import java.io.IOException;
import java.util.LinkedHashMap;
import java.util.List;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;
import org.stellar.sdk.Base64Factory;

/**
 * TransactionResult's original definition in the XDR file is:
 *
 * <pre>
 * struct TransactionResult
 * {
 *     int64 feeCharged; // actual fee charged for the transaction
 *
 *     union switch (TransactionResultCode code)
 *     {
 *     case txFEE_BUMP_INNER_SUCCESS:
 *     case txFEE_BUMP_INNER_FAILED:
 *         InnerTransactionResultPair innerResultPair;
 *     case txSUCCESS:
 *     case txFAILED:
 *         OperationResult results&lt;&gt;;
 *     case txTOO_EARLY:
 *     case txTOO_LATE:
 *     case txMISSING_OPERATION:
 *     case txBAD_SEQ:
 *     case txBAD_AUTH:
 *     case txINSUFFICIENT_BALANCE:
 *     case txNO_ACCOUNT:
 *     case txINSUFFICIENT_FEE:
 *     case txBAD_AUTH_EXTRA:
 *     case txINTERNAL_ERROR:
 *     case txNOT_SUPPORTED:
 *     // case txFEE_BUMP_INNER_FAILED: handled above
 *     case txBAD_SPONSORSHIP:
 *     case txBAD_MIN_SEQ_AGE_OR_GAP:
 *     case txMALFORMED:
 *     case txSOROBAN_INVALID:
 *         void;
 *     }
 *     result;
 *
 *     // reserved for future use
 *     union switch (int v)
 *     {
 *     case 0:
 *         void;
 *     }
 *     ext;
 * };
 * </pre>
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
@Builder(toBuilder = true)
public class TransactionResult implements XdrElement {
  private Int64 feeCharged;
  private TransactionResultResult result;
  private TransactionResultExt ext;

  public void encode(XdrDataOutputStream stream) throws IOException {
    feeCharged.encode(stream);
    result.encode(stream);
    ext.encode(stream);
  }

  public static TransactionResult decode(XdrDataInputStream stream, int maxDepth)
      throws IOException {
    if (maxDepth <= 0) {
      throw new IOException("Maximum decoding depth reached");
    }
    maxDepth -= 1;
    TransactionResult decodedTransactionResult = new TransactionResult();
    decodedTransactionResult.feeCharged = Int64.decode(stream, maxDepth);
    decodedTransactionResult.result = TransactionResultResult.decode(stream, maxDepth);
    decodedTransactionResult.ext = TransactionResultExt.decode(stream, maxDepth);
    return decodedTransactionResult;
  }

  public static TransactionResult decode(XdrDataInputStream stream) throws IOException {
    return decode(stream, XdrDataInputStream.DEFAULT_MAX_DEPTH);
  }

  public static TransactionResult fromXdrBase64(String xdr) throws IOException {
    byte[] bytes = Base64Factory.getInstance().decode(xdr);
    return fromXdrByteArray(bytes);
  }

  public static TransactionResult fromXdrByteArray(byte[] xdr) throws IOException {
    ByteArrayInputStream byteArrayInputStream = new ByteArrayInputStream(xdr);
    XdrDataInputStream xdrDataInputStream = new XdrDataInputStream(byteArrayInputStream);
    xdrDataInputStream.setMaxInputLen(xdr.length);
    return decode(xdrDataInputStream);
  }

  @Override
  public String toJson() {
    return XdrElement.gson.toJson(toJsonObject());
  }

  public static TransactionResult fromJson(String json) {
    return fromJsonObject(XdrElement.gson.fromJson(json, Object.class));
  }

  Object toJsonObject() {
    LinkedHashMap<String, Object> jsonMap = new LinkedHashMap<>();
    jsonMap.put("fee_charged", feeCharged.toJsonObject());
    jsonMap.put("result", result.toJsonObject());
    jsonMap.put("ext", ext.toJsonObject());
    return jsonMap;
  }

  @SuppressWarnings("unchecked")
  static TransactionResult fromJsonObject(Object json) {
    java.util.Map<String, Object> jsonMap = (java.util.Map<String, Object>) json;
    TransactionResult instance = new TransactionResult();
    instance.feeCharged = Int64.fromJsonObject(jsonMap.get("fee_charged"));
    instance.result = TransactionResultResult.fromJsonObject(jsonMap.get("result"));
    instance.ext = TransactionResultExt.fromJsonObject(jsonMap.get("ext"));
    return instance;
  }

  /**
   * TransactionResultResult's original definition in the XDR file is:
   *
   * <pre>
   * union switch (TransactionResultCode code)
   *     {
   *     case txFEE_BUMP_INNER_SUCCESS:
   *     case txFEE_BUMP_INNER_FAILED:
   *         InnerTransactionResultPair innerResultPair;
   *     case txSUCCESS:
   *     case txFAILED:
   *         OperationResult results&lt;&gt;;
   *     case txTOO_EARLY:
   *     case txTOO_LATE:
   *     case txMISSING_OPERATION:
   *     case txBAD_SEQ:
   *     case txBAD_AUTH:
   *     case txINSUFFICIENT_BALANCE:
   *     case txNO_ACCOUNT:
   *     case txINSUFFICIENT_FEE:
   *     case txBAD_AUTH_EXTRA:
   *     case txINTERNAL_ERROR:
   *     case txNOT_SUPPORTED:
   *     // case txFEE_BUMP_INNER_FAILED: handled above
   *     case txBAD_SPONSORSHIP:
   *     case txBAD_MIN_SEQ_AGE_OR_GAP:
   *     case txMALFORMED:
   *     case txSOROBAN_INVALID:
   *         void;
   *     }
   * </pre>
   */
  @Data
  @NoArgsConstructor
  @AllArgsConstructor
  @Builder(toBuilder = true)
  public static class TransactionResultResult implements XdrElement {
    private TransactionResultCode discriminant;
    private InnerTransactionResultPair innerResultPair;
    private OperationResult[] results;

    public void encode(XdrDataOutputStream stream) throws IOException {
      stream.writeInt(discriminant.getValue());
      switch (discriminant) {
        case txFEE_BUMP_INNER_SUCCESS:
        case txFEE_BUMP_INNER_FAILED:
          innerResultPair.encode(stream);
          break;
        case txSUCCESS:
        case txFAILED:
          int resultsSize = getResults().length;
          stream.writeInt(resultsSize);
          for (int i = 0; i < resultsSize; i++) {
            results[i].encode(stream);
          }
          break;
        case txTOO_EARLY:
        case txTOO_LATE:
        case txMISSING_OPERATION:
        case txBAD_SEQ:
        case txBAD_AUTH:
        case txINSUFFICIENT_BALANCE:
        case txNO_ACCOUNT:
        case txINSUFFICIENT_FEE:
        case txBAD_AUTH_EXTRA:
        case txINTERNAL_ERROR:
        case txNOT_SUPPORTED:
        case txBAD_SPONSORSHIP:
        case txBAD_MIN_SEQ_AGE_OR_GAP:
        case txMALFORMED:
        case txSOROBAN_INVALID:
          break;
      }
    }

    public static TransactionResultResult decode(XdrDataInputStream stream, int maxDepth)
        throws IOException {
      if (maxDepth <= 0) {
        throw new IOException("Maximum decoding depth reached");
      }
      maxDepth -= 1;
      TransactionResultResult decodedTransactionResultResult = new TransactionResultResult();
      TransactionResultCode discriminant = TransactionResultCode.decode(stream, maxDepth);
      decodedTransactionResultResult.setDiscriminant(discriminant);
      switch (decodedTransactionResultResult.getDiscriminant()) {
        case txFEE_BUMP_INNER_SUCCESS:
        case txFEE_BUMP_INNER_FAILED:
          decodedTransactionResultResult.innerResultPair =
              InnerTransactionResultPair.decode(stream, maxDepth);
          break;
        case txSUCCESS:
        case txFAILED:
          int resultsSize = stream.readInt();
          if (resultsSize < 0) {
            throw new IOException("results size " + resultsSize + " is negative");
          }
          int resultsRemainingInputLen = stream.getRemainingInputLen();
          if (resultsRemainingInputLen >= 0 && resultsRemainingInputLen < resultsSize) {
            throw new IOException(
                "results size "
                    + resultsSize
                    + " exceeds remaining input length "
                    + resultsRemainingInputLen);
          }
          decodedTransactionResultResult.results = new OperationResult[resultsSize];
          for (int i = 0; i < resultsSize; i++) {
            decodedTransactionResultResult.results[i] = OperationResult.decode(stream, maxDepth);
          }
          break;
        case txTOO_EARLY:
        case txTOO_LATE:
        case txMISSING_OPERATION:
        case txBAD_SEQ:
        case txBAD_AUTH:
        case txINSUFFICIENT_BALANCE:
        case txNO_ACCOUNT:
        case txINSUFFICIENT_FEE:
        case txBAD_AUTH_EXTRA:
        case txINTERNAL_ERROR:
        case txNOT_SUPPORTED:
        case txBAD_SPONSORSHIP:
        case txBAD_MIN_SEQ_AGE_OR_GAP:
        case txMALFORMED:
        case txSOROBAN_INVALID:
          break;
        default:
          throw new IOException("Unknown discriminant value: " + discriminant);
      }
      return decodedTransactionResultResult;
    }

    public static TransactionResultResult decode(XdrDataInputStream stream) throws IOException {
      return decode(stream, XdrDataInputStream.DEFAULT_MAX_DEPTH);
    }

    public static TransactionResultResult fromXdrBase64(String xdr) throws IOException {
      byte[] bytes = Base64Factory.getInstance().decode(xdr);
      return fromXdrByteArray(bytes);
    }

    public static TransactionResultResult fromXdrByteArray(byte[] xdr) throws IOException {
      ByteArrayInputStream byteArrayInputStream = new ByteArrayInputStream(xdr);
      XdrDataInputStream xdrDataInputStream = new XdrDataInputStream(byteArrayInputStream);
      xdrDataInputStream.setMaxInputLen(xdr.length);
      return decode(xdrDataInputStream);
    }

    @Override
    public String toJson() {
      return XdrElement.gson.toJson(toJsonObject());
    }

    public static TransactionResultResult fromJson(String json) {
      return fromJsonObject(XdrElement.gson.fromJson(json, Object.class));
    }

    Object toJsonObject() {
      if (discriminant == TransactionResultCode.txFEE_BUMP_INNER_SUCCESS) {
        LinkedHashMap<String, Object> jsonMap = new LinkedHashMap<>();
        jsonMap.put("txfee_bump_inner_success", innerResultPair.toJsonObject());
        return jsonMap;
      }
      if (discriminant == TransactionResultCode.txFEE_BUMP_INNER_FAILED) {
        LinkedHashMap<String, Object> jsonMap = new LinkedHashMap<>();
        jsonMap.put("txfee_bump_inner_failed", innerResultPair.toJsonObject());
        return jsonMap;
      }
      if (discriminant == TransactionResultCode.txSUCCESS) {
        LinkedHashMap<String, Object> jsonMap = new LinkedHashMap<>();
        jsonMap.put(
            "txsuccess", XdrElement.arrayToJsonArray(results, i -> results[i].toJsonObject()));
        return jsonMap;
      }
      if (discriminant == TransactionResultCode.txFAILED) {
        LinkedHashMap<String, Object> jsonMap = new LinkedHashMap<>();
        jsonMap.put(
            "txfailed", XdrElement.arrayToJsonArray(results, i -> results[i].toJsonObject()));
        return jsonMap;
      }
      if (discriminant == TransactionResultCode.txTOO_EARLY) {
        return "txtoo_early";
      }
      if (discriminant == TransactionResultCode.txTOO_LATE) {
        return "txtoo_late";
      }
      if (discriminant == TransactionResultCode.txMISSING_OPERATION) {
        return "txmissing_operation";
      }
      if (discriminant == TransactionResultCode.txBAD_SEQ) {
        return "txbad_seq";
      }
      if (discriminant == TransactionResultCode.txBAD_AUTH) {
        return "txbad_auth";
      }
      if (discriminant == TransactionResultCode.txINSUFFICIENT_BALANCE) {
        return "txinsufficient_balance";
      }
      if (discriminant == TransactionResultCode.txNO_ACCOUNT) {
        return "txno_account";
      }
      if (discriminant == TransactionResultCode.txINSUFFICIENT_FEE) {
        return "txinsufficient_fee";
      }
      if (discriminant == TransactionResultCode.txBAD_AUTH_EXTRA) {
        return "txbad_auth_extra";
      }
      if (discriminant == TransactionResultCode.txINTERNAL_ERROR) {
        return "txinternal_error";
      }
      if (discriminant == TransactionResultCode.txNOT_SUPPORTED) {
        return "txnot_supported";
      }
      if (discriminant == TransactionResultCode.txBAD_SPONSORSHIP) {
        return "txbad_sponsorship";
      }
      if (discriminant == TransactionResultCode.txBAD_MIN_SEQ_AGE_OR_GAP) {
        return "txbad_min_seq_age_or_gap";
      }
      if (discriminant == TransactionResultCode.txMALFORMED) {
        return "txmalformed";
      }
      if (discriminant == TransactionResultCode.txSOROBAN_INVALID) {
        return "txsoroban_invalid";
      }
      throw new IllegalArgumentException("Unknown discriminant: " + discriminant);
    }

    @SuppressWarnings("unchecked")
    static TransactionResultResult fromJsonObject(Object json) {
      if (json instanceof String) {
        String strVal = (String) json;
        if (!(strVal.equals("txtoo_early")
            || strVal.equals("txtoo_late")
            || strVal.equals("txmissing_operation")
            || strVal.equals("txbad_seq")
            || strVal.equals("txbad_auth")
            || strVal.equals("txinsufficient_balance")
            || strVal.equals("txno_account")
            || strVal.equals("txinsufficient_fee")
            || strVal.equals("txbad_auth_extra")
            || strVal.equals("txinternal_error")
            || strVal.equals("txnot_supported")
            || strVal.equals("txbad_sponsorship")
            || strVal.equals("txbad_min_seq_age_or_gap")
            || strVal.equals("txmalformed")
            || strVal.equals("txsoroban_invalid"))) {
          throw new IllegalArgumentException(
              "Unexpected string '" + strVal + "' for TransactionResultResult");
        }
        TransactionResultResult instance = new TransactionResultResult();
        instance.discriminant = TransactionResultCode.fromJsonObject(strVal);
        return instance;
      }
      java.util.Map<String, Object> jsonMap = (java.util.Map<String, Object>) json;
      if (jsonMap.containsKey("$schema")) {
        jsonMap = new LinkedHashMap<>(jsonMap);
        jsonMap.remove("$schema");
      }
      if (jsonMap.size() != 1) {
        throw new IllegalArgumentException(
            "Expected a single-key object for TransactionResultResult, got: " + json);
      }
      String key = jsonMap.keySet().iterator().next();
      TransactionResultCode discriminant = TransactionResultCode.fromJsonObject(key);
      if (key.equals("txfee_bump_inner_success")) {
        TransactionResultResult instance = new TransactionResultResult();
        instance.discriminant = discriminant;
        instance.innerResultPair =
            InnerTransactionResultPair.fromJsonObject(jsonMap.get("txfee_bump_inner_success"));
        return instance;
      }
      if (key.equals("txfee_bump_inner_failed")) {
        TransactionResultResult instance = new TransactionResultResult();
        instance.discriminant = discriminant;
        instance.innerResultPair =
            InnerTransactionResultPair.fromJsonObject(jsonMap.get("txfee_bump_inner_failed"));
        return instance;
      }
      if (key.equals("txsuccess")) {
        TransactionResultResult instance = new TransactionResultResult();
        instance.discriminant = discriminant;
        instance.results =
            XdrElement.jsonArrayToArray(
                (List<Object>) jsonMap.get("txsuccess"),
                OperationResult.class,
                item -> OperationResult.fromJsonObject(item));
        return instance;
      }
      if (key.equals("txfailed")) {
        TransactionResultResult instance = new TransactionResultResult();
        instance.discriminant = discriminant;
        instance.results =
            XdrElement.jsonArrayToArray(
                (List<Object>) jsonMap.get("txfailed"),
                OperationResult.class,
                item -> OperationResult.fromJsonObject(item));
        return instance;
      }
      throw new IllegalArgumentException("Unknown key '" + key + "' for TransactionResultResult");
    }
  }

  /**
   * TransactionResultExt's original definition in the XDR file is:
   *
   * <pre>
   * union switch (int v)
   *     {
   *     case 0:
   *         void;
   *     }
   * </pre>
   */
  @Data
  @NoArgsConstructor
  @AllArgsConstructor
  @Builder(toBuilder = true)
  public static class TransactionResultExt implements XdrElement {
    private Integer discriminant;

    public void encode(XdrDataOutputStream stream) throws IOException {
      stream.writeInt(discriminant);
      switch (discriminant) {
        case 0:
          break;
      }
    }

    public static TransactionResultExt decode(XdrDataInputStream stream, int maxDepth)
        throws IOException {
      if (maxDepth <= 0) {
        throw new IOException("Maximum decoding depth reached");
      }
      maxDepth -= 1;
      TransactionResultExt decodedTransactionResultExt = new TransactionResultExt();
      Integer discriminant = stream.readInt();
      decodedTransactionResultExt.setDiscriminant(discriminant);
      switch (decodedTransactionResultExt.getDiscriminant()) {
        case 0:
          break;
        default:
          throw new IOException("Unknown discriminant value: " + discriminant);
      }
      return decodedTransactionResultExt;
    }

    public static TransactionResultExt decode(XdrDataInputStream stream) throws IOException {
      return decode(stream, XdrDataInputStream.DEFAULT_MAX_DEPTH);
    }

    public static TransactionResultExt fromXdrBase64(String xdr) throws IOException {
      byte[] bytes = Base64Factory.getInstance().decode(xdr);
      return fromXdrByteArray(bytes);
    }

    public static TransactionResultExt fromXdrByteArray(byte[] xdr) throws IOException {
      ByteArrayInputStream byteArrayInputStream = new ByteArrayInputStream(xdr);
      XdrDataInputStream xdrDataInputStream = new XdrDataInputStream(byteArrayInputStream);
      xdrDataInputStream.setMaxInputLen(xdr.length);
      return decode(xdrDataInputStream);
    }

    @Override
    public String toJson() {
      return XdrElement.gson.toJson(toJsonObject());
    }

    public static TransactionResultExt fromJson(String json) {
      return fromJsonObject(XdrElement.gson.fromJson(json, Object.class));
    }

    Object toJsonObject() {
      if (discriminant == 0) {
        return "v0";
      }
      throw new IllegalArgumentException("Unknown discriminant: " + discriminant);
    }

    @SuppressWarnings("unchecked")
    static TransactionResultExt fromJsonObject(Object json) {
      if (json instanceof String) {
        String strVal = (String) json;
        if (!(strVal.equals("v0"))) {
          throw new IllegalArgumentException(
              "Unexpected string '" + strVal + "' for TransactionResultExt");
        }
        TransactionResultExt instance = new TransactionResultExt();
        instance.discriminant = Integer.parseInt(strVal.substring(1));
        return instance;
      }
      throw new IllegalArgumentException(
          "Expected a string for TransactionResultExt, got: " + json);
    }
  }
}
