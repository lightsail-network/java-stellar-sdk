// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

package org.stellar.sdk.xdr;

import java.io.ByteArrayInputStream;
import java.io.IOException;
import java.util.LinkedHashMap;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;
import org.stellar.sdk.Base64Factory;

/**
 * ContractCostParamEntry's original definition in the XDR file is:
 *
 * <pre>
 * struct ContractCostParamEntry {
 *     // use `ext` to add more terms (e.g. higher order polynomials) in the future
 *     ExtensionPoint ext;
 *
 *     int64 constTerm;
 *     int64 linearTerm;
 * };
 * </pre>
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
@Builder(toBuilder = true)
public class ContractCostParamEntry implements XdrElement {
  private ExtensionPoint ext;
  private Int64 constTerm;
  private Int64 linearTerm;

  public void encode(XdrDataOutputStream stream) throws IOException {
    ext.encode(stream);
    constTerm.encode(stream);
    linearTerm.encode(stream);
  }

  public static ContractCostParamEntry decode(XdrDataInputStream stream, int maxDepth)
      throws IOException {
    if (maxDepth <= 0) {
      throw new IOException("Maximum decoding depth reached");
    }
    maxDepth -= 1;
    ContractCostParamEntry decodedContractCostParamEntry = new ContractCostParamEntry();
    decodedContractCostParamEntry.ext = ExtensionPoint.decode(stream, maxDepth);
    decodedContractCostParamEntry.constTerm = Int64.decode(stream, maxDepth);
    decodedContractCostParamEntry.linearTerm = Int64.decode(stream, maxDepth);
    return decodedContractCostParamEntry;
  }

  public static ContractCostParamEntry decode(XdrDataInputStream stream) throws IOException {
    return decode(stream, XdrDataInputStream.DEFAULT_MAX_DEPTH);
  }

  public static ContractCostParamEntry fromXdrBase64(String xdr) throws IOException {
    byte[] bytes = Base64Factory.getInstance().decode(xdr);
    return fromXdrByteArray(bytes);
  }

  public static ContractCostParamEntry fromXdrByteArray(byte[] xdr) throws IOException {
    ByteArrayInputStream byteArrayInputStream = new ByteArrayInputStream(xdr);
    XdrDataInputStream xdrDataInputStream = new XdrDataInputStream(byteArrayInputStream);
    xdrDataInputStream.setMaxInputLen(xdr.length);
    return decode(xdrDataInputStream);
  }

  @Override
  public String toJson() {
    return XdrElement.gson.toJson(toJsonObject());
  }

  public static ContractCostParamEntry fromJson(String json) {
    return fromJsonObject(XdrElement.gson.fromJson(json, Object.class));
  }

  Object toJsonObject() {
    LinkedHashMap<String, Object> jsonMap = new LinkedHashMap<>();
    jsonMap.put("ext", ext.toJsonObject());
    jsonMap.put("const_term", constTerm.toJsonObject());
    jsonMap.put("linear_term", linearTerm.toJsonObject());
    return jsonMap;
  }

  @SuppressWarnings("unchecked")
  static ContractCostParamEntry fromJsonObject(Object json) {
    java.util.Map<String, Object> jsonMap = (java.util.Map<String, Object>) json;
    ContractCostParamEntry instance = new ContractCostParamEntry();
    instance.ext = ExtensionPoint.fromJsonObject(jsonMap.get("ext"));
    instance.constTerm = Int64.fromJsonObject(jsonMap.get("const_term"));
    instance.linearTerm = Int64.fromJsonObject(jsonMap.get("linear_term"));
    return instance;
  }
}
