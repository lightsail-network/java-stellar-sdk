// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

package org.stellar.sdk.xdr;

import java.io.ByteArrayInputStream;
import java.io.IOException;
import java.util.LinkedHashMap;
import java.util.List;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;
import org.stellar.sdk.Base64Factory;

/**
 * FreezeBypassTxs's original definition in the XDR file is:
 *
 * <pre>
 * struct FreezeBypassTxs {
 *     Hash txHashes&lt;&gt;;
 * };
 * </pre>
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
@Builder(toBuilder = true)
public class FreezeBypassTxs implements XdrElement {
  private Hash[] txHashes;

  public void encode(XdrDataOutputStream stream) throws IOException {
    int txHashesSize = getTxHashes().length;
    stream.writeInt(txHashesSize);
    for (int i = 0; i < txHashesSize; i++) {
      txHashes[i].encode(stream);
    }
  }

  public static FreezeBypassTxs decode(XdrDataInputStream stream, int maxDepth) throws IOException {
    if (maxDepth <= 0) {
      throw new IOException("Maximum decoding depth reached");
    }
    maxDepth -= 1;
    FreezeBypassTxs decodedFreezeBypassTxs = new FreezeBypassTxs();
    int txHashesSize = stream.readInt();
    if (txHashesSize < 0) {
      throw new IOException("txHashes size " + txHashesSize + " is negative");
    }
    int txHashesRemainingInputLen = stream.getRemainingInputLen();
    if (txHashesRemainingInputLen >= 0 && txHashesRemainingInputLen < txHashesSize) {
      throw new IOException(
          "txHashes size "
              + txHashesSize
              + " exceeds remaining input length "
              + txHashesRemainingInputLen);
    }
    decodedFreezeBypassTxs.txHashes = new Hash[txHashesSize];
    for (int i = 0; i < txHashesSize; i++) {
      decodedFreezeBypassTxs.txHashes[i] = Hash.decode(stream, maxDepth);
    }
    return decodedFreezeBypassTxs;
  }

  public static FreezeBypassTxs decode(XdrDataInputStream stream) throws IOException {
    return decode(stream, XdrDataInputStream.DEFAULT_MAX_DEPTH);
  }

  public static FreezeBypassTxs fromXdrBase64(String xdr) throws IOException {
    byte[] bytes = Base64Factory.getInstance().decode(xdr);
    return fromXdrByteArray(bytes);
  }

  public static FreezeBypassTxs fromXdrByteArray(byte[] xdr) throws IOException {
    ByteArrayInputStream byteArrayInputStream = new ByteArrayInputStream(xdr);
    XdrDataInputStream xdrDataInputStream = new XdrDataInputStream(byteArrayInputStream);
    xdrDataInputStream.setMaxInputLen(xdr.length);
    return decode(xdrDataInputStream);
  }

  @Override
  public String toJson() {
    return XdrElement.gson.toJson(toJsonObject());
  }

  public static FreezeBypassTxs fromJson(String json) {
    return fromJsonObject(XdrElement.gson.fromJson(json, Object.class));
  }

  Object toJsonObject() {
    LinkedHashMap<String, Object> jsonMap = new LinkedHashMap<>();
    jsonMap.put(
        "tx_hashes", XdrElement.arrayToJsonArray(txHashes, i -> txHashes[i].toJsonObject()));
    return jsonMap;
  }

  @SuppressWarnings("unchecked")
  static FreezeBypassTxs fromJsonObject(Object json) {
    java.util.Map<String, Object> jsonMap = (java.util.Map<String, Object>) json;
    FreezeBypassTxs instance = new FreezeBypassTxs();
    instance.txHashes =
        XdrElement.jsonArrayToArray(
            (List<Object>) jsonMap.get("tx_hashes"), Hash.class, item -> Hash.fromJsonObject(item));
    return instance;
  }
}
