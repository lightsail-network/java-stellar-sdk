// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

package org.stellar.sdk.xdr;

import java.io.ByteArrayInputStream;
import java.io.IOException;
import java.util.LinkedHashMap;
import java.util.List;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;
import org.stellar.sdk.Base64Factory;

/**
 * FreezeBypassTxsDelta's original definition in the XDR file is:
 *
 * <pre>
 * struct FreezeBypassTxsDelta {
 *     Hash addTxs&lt;&gt;;
 *     Hash removeTxs&lt;&gt;;
 * };
 * </pre>
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
@Builder(toBuilder = true)
public class FreezeBypassTxsDelta implements XdrElement {
  private Hash[] addTxs;
  private Hash[] removeTxs;

  public void encode(XdrDataOutputStream stream) throws IOException {
    int addTxsSize = getAddTxs().length;
    stream.writeInt(addTxsSize);
    for (int i = 0; i < addTxsSize; i++) {
      addTxs[i].encode(stream);
    }
    int removeTxsSize = getRemoveTxs().length;
    stream.writeInt(removeTxsSize);
    for (int i = 0; i < removeTxsSize; i++) {
      removeTxs[i].encode(stream);
    }
  }

  public static FreezeBypassTxsDelta decode(XdrDataInputStream stream, int maxDepth)
      throws IOException {
    if (maxDepth <= 0) {
      throw new IOException("Maximum decoding depth reached");
    }
    maxDepth -= 1;
    FreezeBypassTxsDelta decodedFreezeBypassTxsDelta = new FreezeBypassTxsDelta();
    int addTxsSize = stream.readInt();
    if (addTxsSize < 0) {
      throw new IOException("addTxs size " + addTxsSize + " is negative");
    }
    int addTxsRemainingInputLen = stream.getRemainingInputLen();
    if (addTxsRemainingInputLen >= 0 && addTxsRemainingInputLen < addTxsSize) {
      throw new IOException(
          "addTxs size "
              + addTxsSize
              + " exceeds remaining input length "
              + addTxsRemainingInputLen);
    }
    decodedFreezeBypassTxsDelta.addTxs = new Hash[addTxsSize];
    for (int i = 0; i < addTxsSize; i++) {
      decodedFreezeBypassTxsDelta.addTxs[i] = Hash.decode(stream, maxDepth);
    }
    int removeTxsSize = stream.readInt();
    if (removeTxsSize < 0) {
      throw new IOException("removeTxs size " + removeTxsSize + " is negative");
    }
    int removeTxsRemainingInputLen = stream.getRemainingInputLen();
    if (removeTxsRemainingInputLen >= 0 && removeTxsRemainingInputLen < removeTxsSize) {
      throw new IOException(
          "removeTxs size "
              + removeTxsSize
              + " exceeds remaining input length "
              + removeTxsRemainingInputLen);
    }
    decodedFreezeBypassTxsDelta.removeTxs = new Hash[removeTxsSize];
    for (int i = 0; i < removeTxsSize; i++) {
      decodedFreezeBypassTxsDelta.removeTxs[i] = Hash.decode(stream, maxDepth);
    }
    return decodedFreezeBypassTxsDelta;
  }

  public static FreezeBypassTxsDelta decode(XdrDataInputStream stream) throws IOException {
    return decode(stream, XdrDataInputStream.DEFAULT_MAX_DEPTH);
  }

  public static FreezeBypassTxsDelta fromXdrBase64(String xdr) throws IOException {
    byte[] bytes = Base64Factory.getInstance().decode(xdr);
    return fromXdrByteArray(bytes);
  }

  public static FreezeBypassTxsDelta fromXdrByteArray(byte[] xdr) throws IOException {
    ByteArrayInputStream byteArrayInputStream = new ByteArrayInputStream(xdr);
    XdrDataInputStream xdrDataInputStream = new XdrDataInputStream(byteArrayInputStream);
    xdrDataInputStream.setMaxInputLen(xdr.length);
    return decode(xdrDataInputStream);
  }

  @Override
  public String toJson() {
    return XdrElement.gson.toJson(toJsonObject());
  }

  public static FreezeBypassTxsDelta fromJson(String json) {
    return fromJsonObject(XdrElement.gson.fromJson(json, Object.class));
  }

  Object toJsonObject() {
    LinkedHashMap<String, Object> jsonMap = new LinkedHashMap<>();
    jsonMap.put("add_txs", XdrElement.arrayToJsonArray(addTxs, i -> addTxs[i].toJsonObject()));
    jsonMap.put(
        "remove_txs", XdrElement.arrayToJsonArray(removeTxs, i -> removeTxs[i].toJsonObject()));
    return jsonMap;
  }

  @SuppressWarnings("unchecked")
  static FreezeBypassTxsDelta fromJsonObject(Object json) {
    java.util.Map<String, Object> jsonMap = (java.util.Map<String, Object>) json;
    FreezeBypassTxsDelta instance = new FreezeBypassTxsDelta();
    instance.addTxs =
        XdrElement.jsonArrayToArray(
            (List<Object>) jsonMap.get("add_txs"), Hash.class, item -> Hash.fromJsonObject(item));
    instance.removeTxs =
        XdrElement.jsonArrayToArray(
            (List<Object>) jsonMap.get("remove_txs"),
            Hash.class,
            item -> Hash.fromJsonObject(item));
    return instance;
  }
}
