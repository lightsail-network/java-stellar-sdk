// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

package org.stellar.sdk.xdr;

import java.io.ByteArrayInputStream;
import java.io.IOException;
import java.util.LinkedHashMap;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;
import org.stellar.sdk.Base64Factory;

/**
 * SCPHistoryEntry's original definition in the XDR file is:
 *
 * <pre>
 * union SCPHistoryEntry switch (int v)
 * {
 * case 0:
 *     SCPHistoryEntryV0 v0;
 * };
 * </pre>
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
@Builder(toBuilder = true)
public class SCPHistoryEntry implements XdrElement {
  private Integer discriminant;
  private SCPHistoryEntryV0 v0;

  public void encode(XdrDataOutputStream stream) throws IOException {
    stream.writeInt(discriminant);
    switch (discriminant) {
      case 0:
        v0.encode(stream);
        break;
    }
  }

  public static SCPHistoryEntry decode(XdrDataInputStream stream, int maxDepth) throws IOException {
    if (maxDepth <= 0) {
      throw new IOException("Maximum decoding depth reached");
    }
    maxDepth -= 1;
    SCPHistoryEntry decodedSCPHistoryEntry = new SCPHistoryEntry();
    Integer discriminant = stream.readInt();
    decodedSCPHistoryEntry.setDiscriminant(discriminant);
    switch (decodedSCPHistoryEntry.getDiscriminant()) {
      case 0:
        decodedSCPHistoryEntry.v0 = SCPHistoryEntryV0.decode(stream, maxDepth);
        break;
      default:
        throw new IOException("Unknown discriminant value: " + discriminant);
    }
    return decodedSCPHistoryEntry;
  }

  public static SCPHistoryEntry decode(XdrDataInputStream stream) throws IOException {
    return decode(stream, XdrDataInputStream.DEFAULT_MAX_DEPTH);
  }

  public static SCPHistoryEntry fromXdrBase64(String xdr) throws IOException {
    byte[] bytes = Base64Factory.getInstance().decode(xdr);
    return fromXdrByteArray(bytes);
  }

  public static SCPHistoryEntry fromXdrByteArray(byte[] xdr) throws IOException {
    ByteArrayInputStream byteArrayInputStream = new ByteArrayInputStream(xdr);
    XdrDataInputStream xdrDataInputStream = new XdrDataInputStream(byteArrayInputStream);
    xdrDataInputStream.setMaxInputLen(xdr.length);
    return decode(xdrDataInputStream);
  }

  @Override
  public String toJson() {
    return XdrElement.gson.toJson(toJsonObject());
  }

  public static SCPHistoryEntry fromJson(String json) {
    return fromJsonObject(XdrElement.gson.fromJson(json, Object.class));
  }

  Object toJsonObject() {
    if (discriminant == 0) {
      LinkedHashMap<String, Object> jsonMap = new LinkedHashMap<>();
      jsonMap.put("v0", v0.toJsonObject());
      return jsonMap;
    }
    throw new IllegalArgumentException("Unknown discriminant: " + discriminant);
  }

  @SuppressWarnings("unchecked")
  static SCPHistoryEntry fromJsonObject(Object json) {
    java.util.Map<String, Object> jsonMap = (java.util.Map<String, Object>) json;
    if (jsonMap.containsKey("$schema")) {
      jsonMap = new LinkedHashMap<>(jsonMap);
      jsonMap.remove("$schema");
    }
    if (jsonMap.size() != 1) {
      throw new IllegalArgumentException(
          "Expected a single-key object for SCPHistoryEntry, got: " + json);
    }
    String key = jsonMap.keySet().iterator().next();
    Integer discriminant = Integer.parseInt(key.substring(1));
    if (key.equals("v0")) {
      SCPHistoryEntry instance = new SCPHistoryEntry();
      instance.discriminant = discriminant;
      instance.v0 = SCPHistoryEntryV0.fromJsonObject(jsonMap.get("v0"));
      return instance;
    }
    throw new IllegalArgumentException("Unknown key '" + key + "' for SCPHistoryEntry");
  }
}
