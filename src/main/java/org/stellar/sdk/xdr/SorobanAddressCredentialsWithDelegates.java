// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

package org.stellar.sdk.xdr;

import java.io.ByteArrayInputStream;
import java.io.IOException;
import java.util.LinkedHashMap;
import java.util.List;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;
import org.stellar.sdk.Base64Factory;

/**
 * SorobanAddressCredentialsWithDelegates's original definition in the XDR file is:
 *
 * <pre>
 * struct SorobanAddressCredentialsWithDelegates
 * {
 *     SorobanAddressCredentials addressCredentials;
 *     SorobanDelegateSignature delegates&lt;&gt;;
 * };
 * </pre>
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
@Builder(toBuilder = true)
public class SorobanAddressCredentialsWithDelegates implements XdrElement {
  /**
   * Value of the {@code addressCredentials} field.
   *
   * @param addressCredentials the {@code addressCredentials} field value
   * @return the {@code addressCredentials} field value
   */
  private SorobanAddressCredentials addressCredentials;

  /**
   * Value of the {@code delegates} field.
   *
   * @param delegates the {@code delegates} field value
   * @return the {@code delegates} field value
   */
  private SorobanDelegateSignature[] delegates;

  public void encode(XdrDataOutputStream stream) throws IOException {
    addressCredentials.encode(stream);
    int delegatesSize = getDelegates().length;
    stream.writeInt(delegatesSize);
    for (int i = 0; i < delegatesSize; i++) {
      delegates[i].encode(stream);
    }
  }

  public static SorobanAddressCredentialsWithDelegates decode(
      XdrDataInputStream stream, int maxDepth) throws IOException {
    if (maxDepth <= 0) {
      throw new IOException("Maximum decoding depth reached");
    }
    maxDepth -= 1;
    SorobanAddressCredentialsWithDelegates decodedSorobanAddressCredentialsWithDelegates =
        new SorobanAddressCredentialsWithDelegates();
    decodedSorobanAddressCredentialsWithDelegates.addressCredentials =
        SorobanAddressCredentials.decode(stream, maxDepth);
    int delegatesSize = stream.readInt();
    if (delegatesSize < 0) {
      throw new IOException("delegates size " + delegatesSize + " is negative");
    }
    int delegatesRemainingInputLen = stream.getRemainingInputLen();
    if (delegatesRemainingInputLen >= 0 && delegatesRemainingInputLen < delegatesSize) {
      throw new IOException(
          "delegates size "
              + delegatesSize
              + " exceeds remaining input length "
              + delegatesRemainingInputLen);
    }
    decodedSorobanAddressCredentialsWithDelegates.delegates =
        new SorobanDelegateSignature[delegatesSize];
    for (int i = 0; i < delegatesSize; i++) {
      decodedSorobanAddressCredentialsWithDelegates.delegates[i] =
          SorobanDelegateSignature.decode(stream, maxDepth);
    }
    return decodedSorobanAddressCredentialsWithDelegates;
  }

  public static SorobanAddressCredentialsWithDelegates decode(XdrDataInputStream stream)
      throws IOException {
    return decode(stream, XdrDataInputStream.DEFAULT_MAX_DEPTH);
  }

  public static SorobanAddressCredentialsWithDelegates fromXdrBase64(String xdr)
      throws IOException {
    byte[] bytes = Base64Factory.getInstance().decode(xdr);
    return fromXdrByteArray(bytes);
  }

  public static SorobanAddressCredentialsWithDelegates fromXdrByteArray(byte[] xdr)
      throws IOException {
    ByteArrayInputStream byteArrayInputStream = new ByteArrayInputStream(xdr);
    XdrDataInputStream xdrDataInputStream = new XdrDataInputStream(byteArrayInputStream);
    xdrDataInputStream.setMaxInputLen(xdr.length);
    return decode(xdrDataInputStream);
  }

  @Override
  public String toJson() {
    return XdrElement.gson.toJson(toJsonObject());
  }

  public static SorobanAddressCredentialsWithDelegates fromJson(String json) {
    return fromJsonObject(XdrElement.gson.fromJson(json, Object.class));
  }

  Object toJsonObject() {
    LinkedHashMap<String, Object> jsonMap = new LinkedHashMap<>();
    jsonMap.put("address_credentials", addressCredentials.toJsonObject());
    jsonMap.put(
        "delegates", XdrElement.arrayToJsonArray(delegates, i -> delegates[i].toJsonObject()));
    return jsonMap;
  }

  @SuppressWarnings("unchecked")
  static SorobanAddressCredentialsWithDelegates fromJsonObject(Object json) {
    java.util.Map<String, Object> jsonMap = (java.util.Map<String, Object>) json;
    SorobanAddressCredentialsWithDelegates instance = new SorobanAddressCredentialsWithDelegates();
    instance.addressCredentials =
        SorobanAddressCredentials.fromJsonObject(jsonMap.get("address_credentials"));
    instance.delegates =
        XdrElement.jsonArrayToArray(
            (List<Object>) jsonMap.get("delegates"),
            SorobanDelegateSignature.class,
            item -> SorobanDelegateSignature.fromJsonObject(item));
    return instance;
  }
}
