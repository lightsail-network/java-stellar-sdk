// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

package org.stellar.sdk.xdr;

import java.io.ByteArrayInputStream;
import java.io.IOException;
import java.util.LinkedHashMap;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;
import org.stellar.sdk.Base64Factory;

/**
 * LedgerCloseMetaExt's original definition in the XDR file is:
 *
 * <pre>
 * union LedgerCloseMetaExt switch (int v)
 * {
 * case 0:
 *     void;
 * case 1:
 *     LedgerCloseMetaExtV1 v1;
 * };
 * </pre>
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
@Builder(toBuilder = true)
public class LedgerCloseMetaExt implements XdrElement {
  private Integer discriminant;
  private LedgerCloseMetaExtV1 v1;

  public void encode(XdrDataOutputStream stream) throws IOException {
    stream.writeInt(discriminant);
    switch (discriminant) {
      case 0:
        break;
      case 1:
        v1.encode(stream);
        break;
    }
  }

  public static LedgerCloseMetaExt decode(XdrDataInputStream stream, int maxDepth)
      throws IOException {
    if (maxDepth <= 0) {
      throw new IOException("Maximum decoding depth reached");
    }
    maxDepth -= 1;
    LedgerCloseMetaExt decodedLedgerCloseMetaExt = new LedgerCloseMetaExt();
    Integer discriminant = stream.readInt();
    decodedLedgerCloseMetaExt.setDiscriminant(discriminant);
    switch (decodedLedgerCloseMetaExt.getDiscriminant()) {
      case 0:
        break;
      case 1:
        decodedLedgerCloseMetaExt.v1 = LedgerCloseMetaExtV1.decode(stream, maxDepth);
        break;
      default:
        throw new IOException("Unknown discriminant value: " + discriminant);
    }
    return decodedLedgerCloseMetaExt;
  }

  public static LedgerCloseMetaExt decode(XdrDataInputStream stream) throws IOException {
    return decode(stream, XdrDataInputStream.DEFAULT_MAX_DEPTH);
  }

  public static LedgerCloseMetaExt fromXdrBase64(String xdr) throws IOException {
    byte[] bytes = Base64Factory.getInstance().decode(xdr);
    return fromXdrByteArray(bytes);
  }

  public static LedgerCloseMetaExt fromXdrByteArray(byte[] xdr) throws IOException {
    ByteArrayInputStream byteArrayInputStream = new ByteArrayInputStream(xdr);
    XdrDataInputStream xdrDataInputStream = new XdrDataInputStream(byteArrayInputStream);
    xdrDataInputStream.setMaxInputLen(xdr.length);
    return decode(xdrDataInputStream);
  }

  @Override
  public String toJson() {
    return XdrElement.gson.toJson(toJsonObject());
  }

  public static LedgerCloseMetaExt fromJson(String json) {
    return fromJsonObject(XdrElement.gson.fromJson(json, Object.class));
  }

  Object toJsonObject() {
    if (discriminant == 0) {
      return "v0";
    }
    if (discriminant == 1) {
      LinkedHashMap<String, Object> jsonMap = new LinkedHashMap<>();
      jsonMap.put("v1", v1.toJsonObject());
      return jsonMap;
    }
    throw new IllegalArgumentException("Unknown discriminant: " + discriminant);
  }

  @SuppressWarnings("unchecked")
  static LedgerCloseMetaExt fromJsonObject(Object json) {
    if (json instanceof String) {
      String strVal = (String) json;
      if (!(strVal.equals("v0"))) {
        throw new IllegalArgumentException(
            "Unexpected string '" + strVal + "' for LedgerCloseMetaExt");
      }
      LedgerCloseMetaExt instance = new LedgerCloseMetaExt();
      instance.discriminant = Integer.parseInt(strVal.substring(1));
      return instance;
    }
    java.util.Map<String, Object> jsonMap = (java.util.Map<String, Object>) json;
    if (jsonMap.containsKey("$schema")) {
      jsonMap = new LinkedHashMap<>(jsonMap);
      jsonMap.remove("$schema");
    }
    if (jsonMap.size() != 1) {
      throw new IllegalArgumentException(
          "Expected a single-key object for LedgerCloseMetaExt, got: " + json);
    }
    String key = jsonMap.keySet().iterator().next();
    Integer discriminant = Integer.parseInt(key.substring(1));
    if (key.equals("v1")) {
      LedgerCloseMetaExt instance = new LedgerCloseMetaExt();
      instance.discriminant = discriminant;
      instance.v1 = LedgerCloseMetaExtV1.fromJsonObject(jsonMap.get("v1"));
      return instance;
    }
    throw new IllegalArgumentException("Unknown key '" + key + "' for LedgerCloseMetaExt");
  }
}
