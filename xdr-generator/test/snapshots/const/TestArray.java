// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

package org.stellar.sdk.xdr;

import java.io.IOException;

import org.stellar.sdk.Base64Factory;
import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.List;
import java.util.ArrayList;
import lombok.Data;
import lombok.NoArgsConstructor;
import lombok.AllArgsConstructor;

/**
 * TestArray's original definition in the XDR file is:
 * <pre>
 * typedef int TestArray[FOO];
 * </pre>
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
public class TestArray implements XdrElement {
  private Integer[] TestArray;
  public void encode(XdrDataOutputStream stream) throws IOException {
    int TestArraySize = getTestArray().length;
    if (TestArraySize != Constants.FOO) {
      throw new IOException("TestArray size " + TestArraySize + " does not match fixed size FOO");
    }
    for (int i = 0; i < TestArraySize; i++) {
      stream.writeInt(TestArray[i]);
    }
  }

  public static TestArray decode(XdrDataInputStream stream, int maxDepth) throws IOException {
    if (maxDepth <= 0) {
      throw new IOException("Maximum decoding depth reached");
    }
    maxDepth -= 1;
    TestArray decodedTestArray = new TestArray();
    int TestArraySize = Constants.FOO;
    decodedTestArray.TestArray = new Integer[TestArraySize];
    for (int i = 0; i < TestArraySize; i++) {
      decodedTestArray.TestArray[i] = stream.readInt();
    }
    return decodedTestArray;
  }
  public static TestArray decode(XdrDataInputStream stream) throws IOException {
    return decode(stream, XdrDataInputStream.DEFAULT_MAX_DEPTH);
  }

  public static TestArray fromXdrBase64(String xdr) throws IOException {
    byte[] bytes = Base64Factory.getInstance().decode(xdr);
    return fromXdrByteArray(bytes);
  }

  public static TestArray fromXdrByteArray(byte[] xdr) throws IOException {
    ByteArrayInputStream byteArrayInputStream = new ByteArrayInputStream(xdr);
    XdrDataInputStream xdrDataInputStream = new XdrDataInputStream(byteArrayInputStream);
    xdrDataInputStream.setMaxInputLen(xdr.length);
    return decode(xdrDataInputStream);
  }
  @Override
  public String toJson() {
    return XdrElement.gson.toJson(toJsonObject());
  }

  public static TestArray fromJson(String json) {
    return fromJsonObject(XdrElement.gson.fromJson(json, Object.class));
  }
  Object toJsonObject() {
    return XdrElement.arrayToJsonArray(TestArray, i -> (Integer) TestArray[i]);
  }
  static TestArray fromJsonObject(Object json) {
    TestArray instance = new TestArray();
    instance.TestArray = XdrElement.jsonArrayToArray((List<Object>) json, Integer.class, item -> ((Number) item).intValue());
    return instance;
  }
}
