// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

package org.stellar.sdk.xdr;

import java.io.IOException;

import org.stellar.sdk.Base64Factory;
import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.List;
import java.util.ArrayList;
import lombok.Data;
import lombok.NoArgsConstructor;
import lombok.AllArgsConstructor;
import lombok.Builder;

/**
 * LotsOfMyStructs's original definition in the XDR file is:
 * <pre>
 * struct LotsOfMyStructs
 * {
 *     MyStruct members&lt;&gt;;
 * };
 * </pre>
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
@Builder(toBuilder = true)
public class LotsOfMyStructs implements XdrElement {
  private MyStruct[] members;
  public void encode(XdrDataOutputStream stream) throws IOException{
    int membersSize = getMembers().length;
    stream.writeInt(membersSize);
    for (int i = 0; i < membersSize; i++) {
      members[i].encode(stream);
    }
  }
  public static LotsOfMyStructs decode(XdrDataInputStream stream, int maxDepth) throws IOException {
    if (maxDepth <= 0) {
      throw new IOException("Maximum decoding depth reached");
    }
    maxDepth -= 1;
    LotsOfMyStructs decodedLotsOfMyStructs = new LotsOfMyStructs();
    int membersSize = stream.readInt();
    if (membersSize < 0) {
      throw new IOException("members size " + membersSize + " is negative");
    }
    int membersRemainingInputLen = stream.getRemainingInputLen();
    if (membersRemainingInputLen >= 0 && membersRemainingInputLen < membersSize) {
      throw new IOException("members size " + membersSize + " exceeds remaining input length " + membersRemainingInputLen);
    }
    decodedLotsOfMyStructs.members = new MyStruct[membersSize];
    for (int i = 0; i < membersSize; i++) {
      decodedLotsOfMyStructs.members[i] = MyStruct.decode(stream, maxDepth);
    }
    return decodedLotsOfMyStructs;
  }
  public static LotsOfMyStructs decode(XdrDataInputStream stream) throws IOException {
    return decode(stream, XdrDataInputStream.DEFAULT_MAX_DEPTH);
  }
  public static LotsOfMyStructs fromXdrBase64(String xdr) throws IOException {
    byte[] bytes = Base64Factory.getInstance().decode(xdr);
    return fromXdrByteArray(bytes);
  }

  public static LotsOfMyStructs fromXdrByteArray(byte[] xdr) throws IOException {
    ByteArrayInputStream byteArrayInputStream = new ByteArrayInputStream(xdr);
    XdrDataInputStream xdrDataInputStream = new XdrDataInputStream(byteArrayInputStream);
    xdrDataInputStream.setMaxInputLen(xdr.length);
    return decode(xdrDataInputStream);
  }
  @Override
  public String toJson() {
    return XdrElement.gson.toJson(toJsonObject());
  }

  public static LotsOfMyStructs fromJson(String json) {
    return fromJsonObject(XdrElement.gson.fromJson(json, Object.class));
  }
  Object toJsonObject() {
    LinkedHashMap<String, Object> jsonMap = new LinkedHashMap<>();
    jsonMap.put("members", XdrElement.arrayToJsonArray(members, i -> members[i].toJsonObject()));
    return jsonMap;
  }
  @SuppressWarnings("unchecked")
  static LotsOfMyStructs fromJsonObject(Object json) {
    java.util.Map<String, Object> jsonMap = (java.util.Map<String, Object>) json;
    LotsOfMyStructs instance = new LotsOfMyStructs();
    instance.members = XdrElement.jsonArrayToArray((List<Object>) jsonMap.get("members"), MyStruct.class, item -> MyStruct.fromJsonObject(item));
    return instance;
  }
}
