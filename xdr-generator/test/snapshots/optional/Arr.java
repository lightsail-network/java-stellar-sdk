// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

package org.stellar.sdk.xdr;

import java.io.IOException;

import org.stellar.sdk.Base64Factory;
import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.List;
import java.util.ArrayList;
import lombok.Data;
import lombok.NoArgsConstructor;
import lombok.AllArgsConstructor;

/**
 * Arr's original definition in the XDR file is:
 * <pre>
 * typedef int Arr[2];
 * </pre>
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
public class Arr implements XdrElement {
  private Integer[] Arr;
  public void encode(XdrDataOutputStream stream) throws IOException {
    int ArrSize = getArr().length;
    if (ArrSize != 2) {
      throw new IOException("Arr size " + ArrSize + " does not match fixed size 2");
    }
    for (int i = 0; i < ArrSize; i++) {
      stream.writeInt(Arr[i]);
    }
  }

  public static Arr decode(XdrDataInputStream stream, int maxDepth) throws IOException {
    if (maxDepth <= 0) {
      throw new IOException("Maximum decoding depth reached");
    }
    maxDepth -= 1;
    Arr decodedArr = new Arr();
    int ArrSize = 2;
    decodedArr.Arr = new Integer[ArrSize];
    for (int i = 0; i < ArrSize; i++) {
      decodedArr.Arr[i] = stream.readInt();
    }
    return decodedArr;
  }
  public static Arr decode(XdrDataInputStream stream) throws IOException {
    return decode(stream, XdrDataInputStream.DEFAULT_MAX_DEPTH);
  }

  public static Arr fromXdrBase64(String xdr) throws IOException {
    byte[] bytes = Base64Factory.getInstance().decode(xdr);
    return fromXdrByteArray(bytes);
  }

  public static Arr fromXdrByteArray(byte[] xdr) throws IOException {
    ByteArrayInputStream byteArrayInputStream = new ByteArrayInputStream(xdr);
    XdrDataInputStream xdrDataInputStream = new XdrDataInputStream(byteArrayInputStream);
    xdrDataInputStream.setMaxInputLen(xdr.length);
    return decode(xdrDataInputStream);
  }
  @Override
  public String toJson() {
    return XdrElement.gson.toJson(toJsonObject());
  }

  public static Arr fromJson(String json) {
    return fromJsonObject(XdrElement.gson.fromJson(json, Object.class));
  }
  Object toJsonObject() {
    return XdrElement.arrayToJsonArray(Arr, i -> (Integer) Arr[i]);
  }
  static Arr fromJsonObject(Object json) {
    Arr instance = new Arr();
    instance.Arr = XdrElement.jsonArrayToArray((List<Object>) json, Integer.class, item -> ((Number) item).intValue());
    return instance;
  }
}
